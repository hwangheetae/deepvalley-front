import CommonButton from './components/Common/Button/CommonButton';
const TestPage = () => {
  return (
    <div>
      <p>TestPage 입니다.</p>
      <CommonButton onClick={() => {}} value={'제출하기'} />
    </div>
  );
};

export default TestPage;
