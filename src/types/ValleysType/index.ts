export interface ValleysType {
  name: string;
  valley_id: string;
  thumbnail: string;
  address: string;
  region: string;
  latitude: number;
  longitude: number;
  max_depth: number;
  avg_depth: number;
  tag_names: string[];
  post_count: number;
  rating: string;
  busy: string;
  opened: boolean;
}
